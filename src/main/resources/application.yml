spring:
 main:
  allow-circular-references: true
 datasource:
  url: jdbc:h2:mem:testdb
  initialization-mode: never

 jpa:
  generate-ddl: true
  hibernate:
   ddl-auto: create

  properties:
   hibernate:
    format_sql: true

  show-sql: true


